{% extends 'admin/baseadmin.html.twig' %}

{% form_theme ProduitForm 'bootstrap_4_layout.html.twig' %}

{% block content %}

<div class="col-md-4 mx-auto">
    {{form_start(ProduitForm)}}

        {{ form_row(ProduitForm.category) }}
        {{ form_row(ProduitForm.type) }}
        {{ form_row(ProduitForm.matiere) }}
        {{ form_row(ProduitForm.fabric) }}
        {{ form_row(ProduitForm.distrib) }}
        {{ form_row(ProduitForm.image) }}
        {{ form_row(ProduitForm.content) }}

        <button type="submit" class="btn btn-primary">Ajouter Produit</button>

    {{form_end(ProduitForm)}}
</div>
<div>
{% endblock %}