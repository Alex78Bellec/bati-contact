{% extends 'admin/baseadmin.html.twig' %}

{% block content %}

<div class="admin-page">
        <div class="container">
            <div class="row">
                <div class="col s12">
                    <a href="{{ path('add_produit') }}" class="btn btn blue darken-4" style="float: right">Ajouter</a>
                    <h5>Produits</h5>
                    <table class="table table-dark">

                        <tr>
                            <th>Catégorie</th>
                            <th>Produits</th>
                            <th>Matière</th>
                            <th>Distributeur</th>
                            <th>Fabriquant</th>
                        </tr>
                        {% set i = 1 %}
                        {# {% dump valeur %} #}
                        {% dump produits %}


                {% for produit in produits %}
                <tr>
                    {# {% for category in produits %} #}
                        <td>{{ produit.category }}</td>
                    {# {% endfor %} #}

                    {# {% for type in produits %} #}
                        <td>{{ produit.type }}</td>
                    {# {% endfor %} #}
                    
                    {# {% for matiere in produits %} #}
                        <td>{{ produit.matiere }}</td>
                    {# {% endfor %} #}

                    {% for distrib in distributeurs %}
                    {% for dis in distrib.produits %}
                        <td>{{ distrib.marque}}</td>
                    {% endfor %}
                    {% endfor %}   

                    {# {% for fabric in produits %} #}
                        <td>{{ produit.fabric }}</td>
                    {# {% endfor %} #}
                    </tr>
                {% endfor %}
            
{#                                     <a href="{{ path('article_edit', {'id': produit.id}) }}"><i class="material-icons blue-text">edit</i></a>
                                    <a href="{{ path('article_remove', {'id': produit.id}) }}"><i class="material-icons red-text">delete</i></a> #}
                                
                            {% set i = i+1 %}
                    </table>
                </div>
            </div>

            <div class="row">
                <div class="col s12">
                    <h5>Utilisateurs</h5>
                    <table class="table table-dark table-striped">
                        <tr>
                            <th>#</th>
                            <th>Nom</th>
                            <th>Mot de passe</th>
                            <th>Role</th>
                        </tr>
                        {% set i = 1 %}
                        {% for user in users %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ user.name }}</td>
                                <td>{{ user.password }}</td>
                                <td>{{ user.role }}</td>
{#                                 <td>
                                    <a href=""><i class="material-icons red-text">Supprimer</i></a>
                                    <a href=""><i class="material-icons blue-text">Modifier</i></a>
                                </td> #}
                            </tr>
                            {% set i = i+1 %}
                        {% endfor %}
                    </table>
                </div>
            </div>

            <div class="row">
                <div class="col s12">
                {#     <a href="{{ path('article_add') }}" class="btn btn blue darken-4" style="float: right">Ajouter</a> #}
                    <h5>Distributeur</h5>
                    <table class="table table-dark table-striped">
                        <tr>
                            <th>#</th>
                            <th>Marque</th>
                            <th>Siret</th>
                            <th>Ville</th>
                            <th>Email</th>
                            <th>Téléphone</th>
                        </tr>
                        {% set i = 1 %}
                        {% for distributeur in distributeurs %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ distributeur.marque }}</td>
                                <td>{{ distributeur.siret }}</td>
                                <td>{{ distributeur.ville }}</td>
                                <td>{{ distributeur.email }}</td>
                                <td>{{ distributeur.telephone }}</td>
                                <td>
{#                                     <a href="{{ path('article_edit', {'id': distributeur.id}) }}"><i class="material-icons blue-text">edit</i></a>
                                    <a href="{{ path('article_remove', {'id': distributeur.id}) }}"><i class="material-icons red-text">delete</i></a> #}
                                </td>
                            </tr>
                            {% set i = i+1 %}
                        {% endfor %}
                    </table>
                </div>
            </div>

            <div class="row">
                <div class="col s12">
                {#     <a href="{{ path('article_add') }}" class="btn btn blue darken-4" style="float: right">Ajouter</a> #}
                    <h5>Fabriquant</h5>
                    <table class="table table-dark ">
                        <tr>
                            <th>#</th>
                            <th>Marque</th>
                            <th>Siret</th>
                            <th>Ville</th>
                            <th>Email</th>
                            <th>Téléphone</th>
                        </tr>
                        {% set i = 1 %}
                        {% for fabricant in fabricants %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ fabricant.marque }}</td>
                                <td>{{ fabricant.siret }}</td>
                                <td>{{ fabricant.ville }}</td>
                                <td>{{ fabricant.email }}</td>
                                <td>{{ fabricant.telephone }}</td>
                                <td>
                                    {# <a href="{{ path('article_edit', {'id': fabricant.id}) }}"><i class="material-icons blue-text">edit</i></a>
                                    <a href="{{ path('article_remove', {'id': fabricant.id}) }}"><i class="material-icons red-text">delete</i></a> #}
                                </td>
                            </tr>
                            {% set i = i+1 %}
                        {% endfor %}
                    </table>
                </div>
            </div>

        </div>
    </div>

{% endblock %}