{% extends 'base.html.twig' %}


{% form_theme ProduitFormFab 'bootstrap_4_layout.html.twig' %}

{% block content %}

{% set id = app.user.id %}
{% dump id %}


<div class="col-md-4 mx-auto">
{# {% set fabric = fabricant.user %} #}

    {{form_start(ProduitFormFab)}}

        {{ form_row(ProduitFormFab.category) }}
        {{ form_row(ProduitFormFab.type) }}
        {{ form_row(ProduitFormFab.matiere) }}

        {{ form_row(ProduitFormFab.image) }}
        {{ form_row(ProduitFormFab.content) }}
        {# {{ form_row(ProduitFormFab.fabric) }} #}

        {# //////////////////// #}
        
        {% set varfabid = "" %}


        {% for fabricant in fabricantForm %}
        {% if fabricant.user == app.user %}
        
        {# {% for fabricantName in fabricant.user %} #}
        {% set varfabid = fabricant.id %}
        {% dump varfabid %}
        {# <div style="display:none;"> #}
        {{ form_widget(ProduitFormFab.fabric, {'choices': {'value': varfabid }}) }}
        {# </div> #}

        {# {% endfor %} #}
        {% endif %}
        {% endfor %}

        
        {# //////////////// #}
        <button type="submit" class="btn btn-primary">Ajouter Produit</button>


    {{form_end(ProduitFormFab )}}
</div>
<div>
{% endblock %}